{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python (uv)",
            "type": "debugpy",
            "request": "launch",
            "program": "main.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "VIRTUAL_ENV": "{workspaceFolder}/.venv",
                "PATH": "${env:PATH}:${workspaceFolder}/.venv/bin"
            }
        }
    ]
}